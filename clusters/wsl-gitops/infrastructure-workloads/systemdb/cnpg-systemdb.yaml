apiVersion: postgresql.cnpg.io/v1
kind: Cluster
metadata:
  name: cnpg-systemdb
spec:
  instances: 1
  bootstrap:
    initdb:
      database: cnpg
      owner: cnpg
      secret:
        name: cnpg-systemdb-secret
  storage:
    size: 3Gi
