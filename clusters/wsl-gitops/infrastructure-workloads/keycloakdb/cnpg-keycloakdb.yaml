apiVersion: postgresql.cnpg.io/v1
kind: Cluster
metadata:
  name: cnpg-keycloakdb
spec:
  instances: 1
  bootstrap:
    initdb:
      database: keycloak
      owner: keycloak-user
      secret:
        name: cnpg-keycloakdb-secret
  storage:
    size: 5Gi
